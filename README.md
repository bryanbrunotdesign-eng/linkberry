
  # Rebuild with Linkberry style

  This is a code bundle for Rebuild with Linkberry style. The original project is available at https://www.figma.com/design/1V4W01uv9WuUDgtGeqK4O1/Rebuild-with-Linkberry-style.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  